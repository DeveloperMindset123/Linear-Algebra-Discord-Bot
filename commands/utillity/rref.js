const { SlashCommandBuilder } = require("discord.js");
const math = require("mathjs");
/*
module.exports = {
  data: new SlashCommandBuilder()
    .setName("rref")
    .setName(
      "provide the appropriate coefficient matrix to calculate the rref form"
    )
    .addStringOption((option) =>
      option
        .setName("matrix")
        .setDescription(
          "provide the coefficient matrix of which you want to calculate "
        )
    ),
};
*/
